# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration (MongoDB Atlas)
# Replace with your actual connection string from MongoDB Atlas
# Example (DO NOT COMMIT real credentials):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/agentic-orchestration?retryWrites=true&w=majority
# For local development, you can use:
MONGODB_URI=mongodb://localhost:27017/agentic-orchestration

# Redis Configuration
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-this-in-production

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# AI Provider Configuration (free APIs - placeholders)
# Paste your provider keys here. If you don't have a key for a provider,
# leave the value blank — the service will use fallbacks where possible.
GOOGLE_AI_API_KEY=
GROQ_API_KEY=
HUGGINGFACE_API_KEY=
QWEN_API_KEY=
GLM4_API_KEY=
KIMI_API_KEY=
# OpenRouter provides FREE access to GLM-4.5-Air and Kimi Dev 72B models
OPENROUTER_API_KEY=

# Optional: Puter.js provides client-side free access to some Qwen models
# without server API keys (useful for demos). To use Puter.js in the browser,
# add this script tag to your frontend HTML (do NOT add to server-side .env):
# <script src="https://js.puter.com/v2/"></script>
# Example usage in client JS:
# puter.ai.chat("Your prompt here", { model: "qwen/qwq-32b" })
#   .then(response => puter.print(response));

# NOTE: Never commit your real API keys. Copy this file to `.env` locally
# and add `/server/.env` to your .gitignore if not already ignored.

# Email Configuration (Required for Human Approvals)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Alternative Email Providers:
# For Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# For Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587

# For AWS SES:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587

# Logging Configuration
LOG_LEVEL=debug